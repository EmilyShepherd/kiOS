
type=make

post_build() {
  mkdir -p ${INITRD}/{bin,lib/modules,etc,proc,run,sys,tmp,var/cache,var/lib/kubelet/pods,var/log,var/run,var/tmp,var/meta,var/lib/containers/storage}
  ln -sf bin ${INITRD}/sbin
  cp ${BUILD_DIR}/init ${INITRD}/init
}
